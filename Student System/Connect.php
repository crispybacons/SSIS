<?php
	$hostname = "localhost";
	$username = "root";
	$password = "";
	$database_name = "capstone101";

	$connect = mysqli_connect($hostname, $username, $password, $database_name);

	
	/* if($connect == TRUE) {
		echo "Connection established";
	} else {
		echo "Failed to establish connection";
	}
	*/

?>
